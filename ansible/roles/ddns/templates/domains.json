[
    {
        "name": "{{vault_ddns_domain_name}}",
        "hostname": "{{vault_ddns_loadbalancer_hostname}}"
    },
    {
        "name": "{{vault_ddns_domain_name}}",
        "subdomains": [
            "{{vault_ddns_subdomain_name}}"
        ]
    },
    {
        "name": "{{vault_ddns_domain_name}}",
        "subdomains": [
            "*"
        ],
        "hostname": "{{vault_ddns_loadbalancer_hostname}}"
    }
]
