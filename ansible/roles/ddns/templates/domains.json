[
    {
        "name": "{{vault_ddns.domain_name}}",
        "hostname": "{{vault_ddns.general.loadbalancer.hostname}}"
    },
    {
        "name": "{{vault_ddns.domain_name}}",
        "subdomains": {{vault_ddns.vpn.subdomains | tojson}}
    },
    {
        "name": "{{vault_ddns.domain_name}}",
        "subdomains": {{vault_ddns.general.loadbalancer.subdomains | tojson}},
        "hostname": "{{vault_ddns.general.loadbalancer.hostname}}"
    },
    {
        "name": "{{vault_ddns.domain_name}}",
        "subdomains": {{vault_ddns.iot.loadbalancer.subdomains | tojson}},
        "hostname": "{{vault_ddns.iot.loadbalancer.hostname}}"
    }
]
